# Demo video checklist (3–5 min)

> **Status: to be recorded in Stage 3.** Checklist so the final video hits every
> rubric-visible item.

1. **Review session (desktop):** front (reaction prompt) → Space to flip → draw
   the mechanism (Ketcher structures + curved arrows) → Enter to submit →
   deterministic grade + itemized breakdown → reference mechanism side-by-side →
   keep-for-review / done → next. Show keyboard shortcuts (Space/Enter/1–4/N).
2. **The Rust change in action:** show `TopicMastery` powering the dashboard and
   (if enabled) `points_at_stake` ordering surfacing weak mechanisms; note it's
   computed in the Rust engine (mention `make stage0-proof` / the RPC).
3. **Phone → desktop sync:** review a card on the AnkiDroid build, sync, show it
   appear on desktop (and the reverse).
4. **The three scores with ranges:** Memory (calibrated), Performance, Readiness
   (Chem/Phys 118–132) — each with range, % covered, "how sure", and the give-up
   rule abstaining where data is thin.
5. **AI features:** AI rubric grade with citations; then flip the kill switch and
   show it still grades/scores deterministically (AI-off path).
6. **Test results:** `make eval`, `make bench`, `make leakage` outputs; the
   baseline-comparison table; calibration chart.
